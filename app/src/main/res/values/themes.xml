<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Das Startfenster, bevor Compose zeichnet. Ohne eigene Hintergrundfarbe blitzt hier
        Weiß auf und die App wirkt beim Öffnen billig — der erste Eindruck entsteht in
        diesen zweihundert Millisekunden.
    -->
    <style name="Theme.PeTodo" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Der Teilen-Dialog schwebt über der App, aus der geteilt wurde. -->
    <style name="Theme.PeTodo.Transparent" parent="Theme.PeTodo">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
