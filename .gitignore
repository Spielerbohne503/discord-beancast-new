node_modules/
.screenshots/
*.log
.DS_Store

# Android
android/local.properties
android/.gradle/
android/build/
android/app/build/
*.apk

# Nur für die Auslieferung; entsteht aus dem Wurzelverzeichnis.
dist/

# Der örtliche Zustand von `wrangler dev` — Zwischenstände und ein KV-Speicher,
# der nur zum Prüfen da ist. Gehört niemandem außer diesem Rechner.
.wrangler/
